import { Routes } from '@angular/router';
import { HomeBannerdeespera } from './home-bannerdeespera/home-bannerdeespera';

export const routes: Routes = [
  { path: '', component: HomeBannerdeespera }, // ruta principal
];